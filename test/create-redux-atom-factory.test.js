const { describe, it } = require('mocha')
const { expect } = require('chai')

// const { createReduxAtomFactory } = require('../index')

/**
 *  Create Redux Atom Factory - Test
 */
describe('Create Redux Atom Factory', () => {
  it('should pass the test', () => {
    const expected = true
    const result = true

    expect(result).to.equal(expected)
  })
})
